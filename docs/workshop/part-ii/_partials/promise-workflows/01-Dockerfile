FROM ghcr.io/syntasso/kratix-pipeline-utility:v0.0.1
RUN [ "mkdir", "/templates" ]

COPY resource-configure resource-configure
COPY promise-configure promise-configure

CMD [ "sh", "-c", "./resource-configure"]
ENTRYPOINT []
